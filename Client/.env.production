# Production API base URL.
# Prefer the live Render service. Empty string falls back to this same URL in code.
VITE_API_URL=https://eco-recovery-hub.onrender.com
# Google Maps JavaScript API key — set this in Vercel → Settings → Environment Variables
VITE_GOOGLE_MAPS_KEY=
