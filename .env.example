# Base URL of the standalone backend API (the service in ./server, hosted on Render).
# Example: https://eco-recovery-hub-api.onrender.com
# If left unset, the frontend runs in local-only mode (localStorage) and still works.
VITE_API_URL=
